# Angular runtime env (in deployment these are provided by the platform)
# Base URL for REST API calls
NG_APP_BACKEND_URL=http://localhost:3001

# Optional override for SignalR base URL; if empty the app uses NG_APP_BACKEND_URL
NG_APP_WS_URL=http://localhost:3001

# Optional environment marker
NG_APP_NODE_ENV=development
